# Grimm 동화 웹 크롤러

Grimm 동화 웹사이트(grimmstories.com)에서 동화 내용을 수집하여 JSON 파일로 저장하는 크롤러입니다.

## 설치 방법

### 1. Python 설치 확인
Python 3.7 이상이 설치되어 있어야 합니다.
```bash
python --version
```

### 2. 필요한 라이브러리 설치
```bash
pip install -r requirements.txt
```

## 사용 방법

### 기본 실행
```bash
python grimm_crawler.py
```

실행하면:
1. 메인 페이지에서 모든 동화 링크를 수집합니다
2. 각 동화 페이지에 접속하여 본문 내용을 추출합니다
3. `origin_json` 폴더에 동화별로 개별 JSON 파일을 저장합니다

## 출력 파일 구조

`origin_json/` 폴더에 각 동화가 개별 JSON 파일로 저장됩니다:

```
origin_json/
├── Little Red Riding Hood.json
├── Hansel and Gretel.json
├── Cinderella.json
└── ...
```

각 JSON 파일의 형식:

```json
{
  "title": "동화 제목",
  "url": "동화 페이지 URL",
  "content": "동화 본문 내용..."
}
```

## 주의사항

- 웹사이트 서버에 부담을 주지 않기 위해 각 요청 사이에 1초의 대기 시간이 있습니다
- 웹사이트 구조가 변경되면 코드 수정이 필요할 수 있습니다
- 인터넷 연결이 필요합니다

## 문제 해결

만약 동화 내용이 제대로 추출되지 않는다면:
1. 웹사이트를 직접 방문하여 HTML 구조를 확인하세요
2. `extract_story_content()` 함수의 선택자를 수정하세요

## 💡 추가 팁

1. **저장 폴더명 변경**: `main()` 함수에서 `output_folder = 'origin_json'` 을 원하는 폴더명으로 수정
2. **특정 동화만 수집**: `story_links` 리스트를 슬라이싱 (예: `story_links[:10]` - 처음 10개만)
3. **대기 시간 조정**: `time.sleep(1)` 의 숫자 변경 (단위: 초)
4. **파일명 규칙**: 동화 제목이 파일명으로 사용되며, 특수문자는 자동으로 제거됩니다

